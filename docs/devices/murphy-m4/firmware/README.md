# Архив прошивок Murphy

Локальные копии заводских прошивок. Хранятся в репозитории потому, что источники ненадёжны:
`murphy.pandacat.ai` в манифесте `/ota/latest` называет **только текущую** версию, а указателя на
прежние сборки не публикует нигде. Сами файлы по старым прямым адресам пока отдаются (проверено
29.08.2026: `panda-ai-os-2.2.7.bin` вернул HTTP 200 через недели после выхода 2.3.5), но узнать
такой адрес можно только заранее записав его.

⚠️ Это **чужие проприетарные бинарники**, выложенные их авторами в открытый доступ. Здесь они
лежат как исходный материал для реверс-инжиниринга и как средство восстановления устройства.

## Содержимое

| Файл | Размер | Для чего | Откуда |
|---|---:|---|---|
| `panda-ai-os-2.3.5.bin` | 4 319 904 | **Panda AI OS**, актуальная на 29.08.2026. Печатает всю распиновку в лог при загрузке; в ней константы полнее, чем в 2.2.7 | `https://murphy.pandacat.ai/firmware/panda-ai-os-2.3.5.bin` (адрес получен из `/ota/latest`) |
| `panda-ai-os-2.2.7.bin` | 6 009 744 | Предыдущая Panda AI OS (была актуальной 07.08.2026). **Панель 800×480 → это M4.** Из неё впервые вынута вся распиновка | `https://murphy.pandacat.ai/firmware/panda-ai-os-2.2.7.bin` |
| `murphy-26-0526-1.2.16.bin` | 3 903 024 | Предыдущая MurphyOS («Murphy Reader» v1.2.16, сборка 2026-05-26). Та, на которой команда CrossPoint доказала родство с их кодом | `crosspoint-reader/Murphy`, файл `m4/` |
| `mofei-corogoo-EPD426-v1.bin` | 5 626 704 | **Заводская OEM-прошивка MoFei/corogoo для 4.26"** — альтернатива MurphyOS на том же железе | `crosspoint-reader/Murphy`, файл `m3/oem_firmware/` (лежит там как «старший брат», несмотря на путь) |
| `mofei-corogoo-touch-v525.bin` | 2 907 200 | Заводская OEM для **3.7" (M3)**. Держим для сравнения: по ней видно, что отличается между моделями | `crosspoint-reader/Murphy`, файл `m3/oem_firmware/` |

## Контрольные суммы (SHA-256)

```
d049bfc8e5d44c8f0be6c8f69855a9b36954e3df053ffecaa5b86dfe449dfebd  panda-ai-os-2.3.5.bin
089b10973938bdf5f05b8910853a851a1cae586ce68516a50bc4c6871e44c458  panda-ai-os-2.2.7.bin
3ee3d0a7207a17d49eb47fa60febff8cf4ac2f47bb52d74a1beceb40b8b124ea  murphy-26-0526-1.2.16.bin
e932fbc697339f3b1e17adcb6a77e11ba50a1ec2297c20a4fbf30a667759fa50  mofei-corogoo-EPD426-v1.bin
```

Все четыре сверены с манифестами и с заметками репозитория Murphy. Проверить:

```bash
shasum -a 256 *.bin
```

## Чего здесь нет и почему

- **Дампа собственного устройства.** Появится после получения M4 — но полный дамп содержит
  раздел NVS с паролями Wi-Fi, поэтому в репозиторий он попадёт только после очистки
  `0x9000`–`0xE000`, либо не попадёт вовсе.
- **Сборок CrossPoint для M3** (релиз `murphy-m3-v1`) — они не наши и на M4 непригодны.

## Как этим пользоваться

Разбор и извлечённые константы — в [../firmware-analysis.md](../firmware-analysis.md),
скрипты — в [../tools/](../tools/). Быстрый старт:

```bash
python3 ../tools/espimg.py panda-ai-os-2.3.5.bin
```

Для восстановления устройства эти файлы напрямую **не подходят**: OTA-образ пишется не с
нулевого адреса, а с `app_offset` (у MurphyOS — `0x20000`), и стоковая ОС дополнительно
требует код активации по UID. Надёжный способ отката — собственный дамп всей флеш-памяти,
снятый до экспериментов.
